import { Neutron } from "../Neutron/Neutron.js";
export class Events extends Neutron.Events {
    onClick(e) {
        switch (e.key) {
            case ``:
                break;
        }
    }
    offClick(e) {
        switch (e.key) {
            case ``:
                break;
        }
    }
    mouseDown(e) {
    }
    mouseMove(e) {
    }
}
